// import libs
import { Outlet } from 'react-router-dom';

// ==============================|| LANDING - MAIN CLIENT LAYOUT ||============================== //

const MainClient = () => {
    return (
        <>
            <Outlet />
        </>
    );
};

export default MainClient;
